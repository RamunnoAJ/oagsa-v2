export class SellCondition {
  /**
   * @typedef SellCondition
   * @param {number} id
   * @param {string} description
   * @param {string} type
   * */
  constructor(id, description, type) {
    this.id = id
    this.description = description
    this.type = type
  }
}
